<!DOCTYPE html>
<html lang="es" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Registro de Usuarios - Prestabank</title>
    <link href="https://cdn.jsdelivr.net/npm/flowbite@2.5.2/dist/flowbite.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50 flex flex-col min-h-screen">

<!-- Navbar -->
<nav class="bg-blue-900 p-4 text-white shadow-md">
    <div class="flex justify-between items-center">
        <h1 class="text-3xl font-bold">Prestabank</h1>
        <ul class="flex gap-6">
            <li><a href="#" class="hover:text-gray-300 transition-colors">Inicio</a></li>
            <li><a th:href="@{formulario}" class="hover:text-gray-300 transition-colors">Formulario Empleados</a></li>
            <li><a th:href="@{registroCuenta}" class="hover:text-gray-300 transition-colors">Registro Cuenta</a></li>
            <li><a href="#contacto" class="hover:text-gray-300 transition-colors">Contacto</a></li>
            <li><a th:href="@{atencionCliente}" class="hover:text-gray-300 transition-colors">Atención a Clientes</a></li>
            <li><a th:href="@{informacionCliente}" class="hover:text-gray-300 transition-colors">Información del Cliente</a></li>
        </ul>
    </div>
</nav>

<!-- Contenedor Principal -->
<main class="flex-grow">
    <!-- Formulario de Registro -->
    <section class="py-20 flex flex-col items-center justify-center text-center">
        <div class="container flex flex-col items-center justify-center text-center mt-12 p-8 bg-gray-100">
            <h2 class="text-3xl font-bold text-blue-900 text-center mb-12">Registro de Usuarios</h2>

            <!-- Contenedor flexible para formulario y carta -->
            <div class="flex flex-col md:flex-row gap-6 items-center justify-center">
                <!-- Formulario -->
                <div class="max-w-2xl bg-white p-8 rounded-lg shadow-lg">
                    <form th:action="@{/RegistroCuenta}" method="post" >
                        <input type="hidden" id="clienteId" name="clienteId" th:value="${param.id}">
                        <div class="mb-6">
                            <label for="nombre" class="block text-sm font-medium text-gray-700">Usuario</label>
                            <input type="text" id="nombre" name="nombre" class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" required>
                        </div>
                        <div class="mb-6">
                            <label for="password" class="block text-sm font-medium text-gray-700">Contraseña</label>
                            <input type="password" id="password" name="password" class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" required>
                        </div>
                        <div class="mb-6">
                            <label for="confirmPassword" class="block text-sm font-medium text-gray-700">Confirmar Contraseña</label>
                            <input type="password" id="confirmPassword" name="confirmPassword" class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" required>
                        </div>
                        <div class="flex justify-center">
                            <button type="submit" class="bg-blue-700 text-white px-6 py-3 rounded-lg hover:bg-blue-800 transition-colors">Registrarse</button>
                        </div>
                    </form>
                </div>

                <!-- Carta informativa -->
                <div class="bg-white p-6 shadow-lg rounded-lg transition transform hover:scale-105 hover:bg-blue-100 hover:shadow-xl hover:border-2 hover:border-blue-400 max-w-sm">
                    <h3 class="font-semibold text-lg text-blue-900">Advertencias y Recomendaciones</h3>
                    <p class="text-gray-700 mt-2">Al registrar un nuevo usuario, se garantiza la seguridad de tus datos personales.</p>
                    <p class="text-gray-700 mt-2">Prestabank se compromete a mantener la confidencialidad de la información personal y financiera de sus clientes.</p>
                    <img src="imagenes/logo_azul.png" class="w-full h-40 object-cover mt-4 rounded-lg">
                </div>
            </div>
        </div>
    </section>
</main>

<!-- Footer -->
<footer id="contacto" class="bg-blue-900 text-white text-center py-6 mt-auto">
    <p>&copy; 2025 Prestabank. Todos los derechos reservados.</p>
    <p>Dirección: Av. Siempre Viva 123, Quito, Ecuador | Teléfono: (02) 123-4567</p>
</footer>

<script src="https://cdn.jsdelivr.net/npm/flowbite@2.5.2/dist/flowbite.min.js"></script>

</body>
</html>
